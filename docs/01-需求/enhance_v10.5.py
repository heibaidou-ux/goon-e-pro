"""
Enhance V10.5 based on Claude review feedback, picking the genuinely valuable parts.
"""
filepath = r"C:\Users\王晓东\Documents\高岸管理\盈隆\高岸智能管理系统\高岸ERP\docs\01-需求\高岸ERP系统-需求说明书（V10.0，2026年5月6日）.md"

with open(filepath, 'r', encoding='utf-8') as f:
    content = f.read()

original = content
ops = []

# ====================================================================
# 1. Enhance 3.5.4 智能对账 - add platform reconciliation details
# ====================================================================
reconcile_enhance = """

**平台对账口径说明**：

各平台结算规则存在差异，对账系统需针对不同平台配置独立对账口径：

**美团对账口径**：
- 美团"茶室"类目与"CAFE"类目的结算科目不同：茶室类目按"房间费+商品费"合并结算，CAFE类目按单品结算
- 美团结算单中的"平台服务费"包含佣金和营销费用，需按比例拆分入账
- 优惠券/团购券核销金额 = 客人实付 + 平台补贴，系统需分别确认
- 查询路径：美团开放平台 → 数据中心 → 结算管理 → 结算记录 → 下载结算单

**抖音对账口径**：
- 抖音核销后T+1生成结算单，含订单金额、平台佣金、达人佣金、平台补贴
- 注意区分"达人带货佣金"（流水抽成）和"平台技术服务费"（固定比例）
- 查询路径：抖音开放平台 → 数据 → 结算中心 → 店铺结算

**支付宝对账口径**：
- 支付宝商户平台 → 对账中心 → 下载交易账单

**对账明细字段对照表**：

| 系统字段 | 美团结算单字段 | 抖音结算单字段 | 说明 |
|---------|-------------|-------------|------|
| 订单编号 | 订单ID | 订单号 | 三方订单号的本地映射 |
| 交易时间 | 交易完成时间 | 核销时间 | 以三方平台时间为准 |
| 商品名称 | 商品/套餐名称 | 商品标题 | 用于匹配确认 |
| 订单金额 | 订单金额 | 商品总额 | 用户下单时支付金额 |
| 平台服务费 | 平台服务费 | 平台技术服务费 | 平台抽成 |
| 营销费用 | 商家营销承担 | 商家补贴 | 优惠券/满减等 |
| 达人佣金 | — | 达人佣金 | 仅抖音 |
| 平台补贴 | 平台补贴 | 平台补贴 | 平台活动补贴 |
| 结算金额 | 结算金额 | 结算金额 | 实际到账金额 = 订单金额 - 平台服务费 - 营销费用 - 达人佣金 + 平台补贴 |
| 结算周期 | T+1 | T+1 | 各平台均为次日结算 |

月结时以各平台结算单汇总金额与系统收入流水进行汇总比对（总额对账），确保账实一致。
"""

# Insert after the 业务规则 section of 3.5.4
insert_point = content.find("月结前必须处理完所有工单，否则月结报告标记异常")
if insert_point != -1:
    insert_point += len("月结前必须处理完所有工单，否则月结报告标记异常")
    content = content[:insert_point] + "\n" + reconcile_enhance + content[insert_point:]
    ops.append("Added platform reconciliation details (美团/抖音/支付宝 对账口径 + 字段对照表)")
else:
    print("WARNING: 3.5.4 insertion point not found!")

# ====================================================================
# 2. Enhance 3.5.3 自动月结 - add monthly report field mapping
# ====================================================================
monthly_report_table = """

**月结报告结构（Excel输出格式）**：

月结报告包含以下工作表，是财务对外输出的标准格式：

**工作表1：收入明细**

| 字段 | 说明 | 数据来源 |
|------|------|---------|
| 门店名称 | 高岸分店 | 系统门店信息 |
| 日期 | 日结日期 | 系统日结记录 |
| 订单编号 | 系统订单号 | ERP订单系统 |
| 平台 | 美团/抖音/支付宝/微信/现金 | 支付渠道 |
| 商品/服务 | 具体消费项目 | 订单明细 |
| 订单金额 | 客人下单总金额 | 订单金额 |
| 平台优惠 | 平台承担优惠 | 平台结算单 |
| 商家优惠 | 商家承担优惠 | ERP优惠券系统 |
| 平台服务费 | 平台抽成 | 平台结算单 |
| 达人佣金 | 达人带货分成（仅抖音） | 抖音结算单 |
| 实收金额 | 最终到账金额 | 平台结算单 |
| 支付时间 | 实际支付时间 | 支付回调 |
| GL凭证号 | 关联会计凭证号 | GL系统 |

**工作表2：支出明细**

| 字段 | 说明 | 数据来源 |
|------|------|---------|
| 门店名称 | 高岸分店 | 系统门店信息 |
| 日期 | 支出日期 | 支出单据 |
| 单据编号 | 申请单号/报销单号 | 支出管理系统 |
| 支出科目 | 一级/二级科目 | 科目表配置 |
| 供应商/收款人 | 收款方名称 | 供应商/员工信息 |
| 金额 | 含税金额 | 支出单据 |
| 税率 | 增值税率 | 税务配置 |
| 不含税金额 | 税前金额 | 自动计算 |
| 税额 | 增值税额 | 自动计算 |
| 支付方式 | 对公转账/备用金/报销 | 支付方式 |
| 支付状态 | 已支付/待支付/审批中 | 支出管理 |
| GL凭证号 | 关联会计凭证号 | GL系统 |

**工作表3：损益汇总**

| 字段 | 金额 | 说明 |
|------|------|------|
| 营业收入 | 汇总 | 各平台实收金额 + 线下收款 |
| 减：平台服务费 | 汇总 | 各平台抽成合计 |
| 减：营销费用 | 汇总 | 优惠券补贴 + 满减活动 |
| 减：原材料成本 | 汇总 | 当期消耗物料成本 |
| 减：人力成本 | 汇总 | 工资 + 社保 + 公积金 |
| 减：折旧费用 | 汇总 | 固定资产折旧 |
| 减：运营费用 | 汇总 | 房租、水电、物业等 |
| 营业利润 | = 收入 - 各项费用 | — |
| 加：平台补贴收入 | 汇总 | 平台营销补贴 |
| 加：品牌特许经营收入 | 汇总 | 加盟金/品牌管理费 |
| 净利润 | = 营业利润 + 补贴 + 特许收入 | — |
"""

# Find 自动月结 section - insert before the closing --- separator
insert_point = content.find("\n\n---\n\n\n---\n\n#### 3.5.4")
if insert_point != -1:
    content = content[:insert_point] + "\n" + monthly_report_table + content[insert_point:]
    ops.append("Added monthly report Excel structure (3 sheets: income detail, expense detail, P&L summary)")
else:
    print("WARNING: 3.5.3 insertion point not found!")

# ====================================================================
# 3. Enhance AI预警 with quantified thresholds (3.7.3)
# ====================================================================
ai_enhance = """

**阈值配置说明**：
- 以上阈值均为系统默认值，支持按门店独立配置
- 环比计算周期：默认7天滚动环比，可切换为30天
- 预警频率：同一类型同一门店24小时内仅触发一次
- 预警升级：触发后4小时未被处理，升级推送至上一级管理员
- 静默期：已标记"已处理"的同类型预警，7天内不重复触发

**预警处理SLA**：
| 预警类型 | 响应时限 | 升级条件 |
|---------|---------|---------|
| 营收异常下降 | 2小时 | 超时 → 推送至总部运营总监 |
| 成本异常上升 | 4小时 | 超时 → 推送至财务总监 |
| 对账大额差异 | 1小时 | 超时 → 推送至财务经理 |
| 库存积压 | 4小时 | 超时 → 推送至供应链经理 |
| 设备批量离线 | 30分钟 | 超时 → 推送至技术负责人 |
| 客流异常 | 2小时 | 超时 → 推送至市场总监 |
| 差评预警 | 1小时 | 超时 → 推送至运营总监 |
"""

# Insert after the 预警类型 table (after the 差评预警 row)
insert_point = content.find("| 差评预警 | 平台差评数量超过阈值 | 店长、总部 |")
if insert_point != -1:
    insert_point = content.find("\n\n", insert_point)  # After the table end
    if insert_point == -1:
        insert_point = content.find("\n**操作流程**", content.find("| 差评预警"))
    content = content[:insert_point] + "\n" + ai_enhance + "\n" + content[insert_point:]
    ops.append("Enhanced AI预警 with quantified thresholds, SLA, and escalation rules")
else:
    print("WARNING: AI预警 insertion point not found!")

# ====================================================================
# 4. Add 待决策事项清单 before revision history
# ====================================================================
decision_log = """
---

## 附录B：待决策事项清单

以下事项需要在系统开发启动前由项目决策方明确，以便在需求中锁定范围：

| 编号 | 事项 | 提出方 | 建议方案 | 决策截止 | 状态 |
|------|------|-------|---------|---------|------|
| D-01 | 多门店统一会员体系 vs 一店一会员 | 运营部 | 建议统一会员体系（跨店积分通兑），但需评估各店独立核算的复杂度 | 需求评审前 | ⏳ 待决策 |
| D-02 | 平台补贴是否纳入主营收入科目 | 财务部 | 建议单列为"其他业务收入-平台补贴收入"，不影响毛利率计算 | 需求评审前 | ⏳ 待决策 |
| D-03 | 加盟店是否必须使用总部ERP系统 | 总经办 | 建议强制使用，确保数据贯通和管理费自动计算 | 需求评审前 | ⏳ 待决策 |
| D-04 | 投资人是否可以查看门店实时流水 | 法务 | 建议投资人仅查看月度汇总数据，不暴露日流水级别的经营细节 | 需求评审前 | ⏳ 待决策 |
| D-05 | GL凭证生成时机：实时 vs 日结批量 | 财务部 | 建议交易级实时生成，便于日终对账 | 需求评审前 | ⏳ 待决策 |
| D-06 | 固定资产折旧方法：直线法 vs 加速折旧 | 财务部 | 建议统一采用直线法（年限平均法），简化折旧计算 | 需求评审前 | ⏳ 待决策 |
| D-07 | 智能排班是否与考勤数据自动挂钩 | HR | 建议自动挂钩，排班数据直接作为考勤基准，减少重复录入 | 需求评审前 | ⏳ 待决策 |
| D-08 | 加盟商端小程序开发优先级 | 总经办 | 建议首期不包含，作为V2.0规划 | 需求评审前 | ⏳ 待决策 |
| D-09 | 品牌管理费收取方式：按月固定 vs 按营收比例 | 总经办 | 建议按营收比例（2%-5%），与加盟店经营状况挂钩 | 需求评审前 | ⏳ 待决策 |
| D-10 | 数据迁移策略：历史数据是否导入新系统 | 技术部 | 建议仅导入近12个月数据，更早数据归档后离线查询 | 需求评审前 | ⏳ 待决策 |

> 说明：上表所列事项涉及业务流程或财务处理的核心策略选择，需由相关方在开发启动前明确决策。未决策事项将在开发过程中按"默认方案"先行实现，后续变更可能产生返工成本。
"""

# Find the revision history section - insert before it
revision_header = content.find("## 修订历史")
if revision_header != -1:
    # Find the end of the preceding section's --- separator
    # Go back to find where the --- is before 修订历史
    before_revision = content.rfind("---", 0, revision_header)
    if before_revision != -1:
        # Check if 附录 already exists
        if "附录B" not in content and "待决策事项" not in content:
            content = content[:before_revision] + decision_log + "\n" + content[before_revision:]
            ops.append("Added 待决策事项清单 (10 items) before revision history")
        else:
            ops.append("SKIP: 待决策事项清单 already exists")
else:
    print("WARNING: 修订历史 not found!")

# ====================================================================
# Save
# ====================================================================
if content != original:
    with open(filepath, 'w', encoding='utf-8') as f:
        f.write(content)
    print("\nAll changes applied successfully!\n")
else:
    print("\nNo changes made!\n")

for op in ops:
    print(f"  + {op}")
